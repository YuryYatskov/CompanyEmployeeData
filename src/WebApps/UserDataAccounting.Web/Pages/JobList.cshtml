@page
@model UserDataAccounting.Web.Pages.JobListModel
@{
    ViewData["Title"] = "Job";
}

<div class="container">
    <div class="row">
        <div class="col">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a asp-page="Index">Home</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Job</li>
                </ol>
            </nav>
        </div>
    </div>
</div>

<div class="container">
    <div class="row">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Id</th>
                    <th scope="col" class="text-center">Actions</th>
                </tr>
            </thead>
            <tbody>

                @foreach (var job in Model.JobList)
                {
                    <tr>
                        <td>@job.Name</td>
                        <td>@job.Id</td>
                        <td class="text-center">
                            <form method="post">
                                <a asp-page="JobDetail" asp-route-id="@job.Id" class="btn btn-sm btn-warning">Edit</a>
                                <button type="submit" asp-page-handler="DeleteJob" asp-route-id="@job.Id" class="btn btn-danger btn-sm">Delete</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <div class="text-center">
            <a asp-page="JobDetail" class="btn btn-lg btn-block btn-success">Create job</a>
        </div>
    </div>
</div>